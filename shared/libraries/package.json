{
  "name": "@event-foundry/libraries",
  "version": "0.1.0",
  "private": true,
  "description": "Utilitaires transverses partagés entre composants EventFoundry (sans logique métier).",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
