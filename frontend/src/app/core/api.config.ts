/**
 * Base de l'API REST. En dev, le backend tourne sur :3000 (CORS autorise :4200).
 * En production, remplacer par l'URL du backend derrière le même domaine.
 */
export const API_BASE = 'http://localhost:3000/api/v1';
